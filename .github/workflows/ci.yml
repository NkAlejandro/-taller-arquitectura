name: CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Simular build
        run: echo "Construyendo proyecto..."

  test:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Simular test
        run: echo "Ejecutando pruebas..."

      - name: Deploy simulado
        run: echo "Despliegue listo"
